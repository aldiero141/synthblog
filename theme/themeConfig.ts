import type { ThemeConfig } from "antd";

const theme: ThemeConfig = {
  components: {
    Layout: {
      headerBg: "#ffffff",
    },
  },
  token: {
    fontSize: 16,
    colorPrimary: "#52c41a",
    fontFamily: "Segoe UI",
  },
};

export default theme;
