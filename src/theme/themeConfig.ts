import type { ThemeConfig } from "antd";

const theme: ThemeConfig = {
  components: {
    Layout: {
      headerBg: "#ffffff",
    },
  },
  token: {
    fontSize: 16,
    colorPrimary: "#3c89e8",
    fontFamily: "Noto Sans",
  },
};

export default theme;
